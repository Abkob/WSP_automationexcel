openpyxl>=3.1,<4
pandas>=3,<4
pytest>=9,<10
SQLAlchemy>=2,<3
fastapi>=0.136,<1
uvicorn>=0.49,<1
plotly>=5,<7
sentence-transformers>=3,<4
faiss-cpu>=1.8,<2
pystray>=0.19,<1
Pillow>=10,<12

# Planned core app dependencies, installed when their phases begin:
# watchdog>=4
#
# Semantic search embeds student profiles locally with sentence-transformers and stores vectors in FAISS.
# Qwen/Ollama is optional for query rewriting and explanations.
